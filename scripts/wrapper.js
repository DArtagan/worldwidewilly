var.wrapper = document.createElement('div'); 
var myDiv = document.getElementByClass('myDiv'); 
wrapper.appendChild(myDiv.cloneNode(true)); 
myDiv.parentNode.replaceChild.wrapper, myDiv); 

var div = new Element('div', { 'class': 'table-wrapper' });
$('data').wrap(div);

$("* :contains('divider')").wrap("<div class='bar'></div>");
$('div.bar').each(function(){ $(this).after( $(this).text() ); }).remove();


document.write(str.replace(/microsoft/gi, "W3Schools"));
